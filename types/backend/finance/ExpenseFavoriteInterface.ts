export interface ExpenseFavoriteInterface {
  id: number;
  userId: string;
  place: string;
  placeId: number;
  country: string;
  itemDescription: string;
  unitPrice: number;
  payment: string;
}
