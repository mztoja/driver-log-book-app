export enum serviceTypeEnum {
  maintenance = 0,
  service = 1,
}

// UI-only: który wariant formularza serwisu wywołano z ekranu home.
export enum ServiceEnum {
  standard = 0,
  fifthWheelLube = 1,
}
